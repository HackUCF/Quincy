#!/usr/bin/env -S air -c
# run with:
#   air -c api.air.toml
# or 
#   ./api.air.toml
[build]
  cmd = "go build -o quincy ."
  entrypoint = ["./quincy", "api", "start"]
  exclude_dir = ["agent"]
  include_ext = ["go", "sql", "html", "yaml"]